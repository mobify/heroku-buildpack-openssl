#!/usr/bin/env bash
# bin/release <build-dir>

BIN_DIR=$(cd $(dirname $0); pwd) # absolute path
BUILD_DIR=$1

cat <<EOF
---
config_vars:
    PATH=${BUILD_DIR}/.heroku/vendor/bin:$PATH
    PKG_CONFIG_PATH=${BUILD_DIR}/.heroku/vendor/lib/pkgconfig:${PKG_CONFIG_PATH}
    INCLUDE_PATH=${BUILD_DIR}/.heroku/vendor/include:${BUILD_DIR}/.heroku/vendor/include/openssl:${INCLUDE_PATH}
    LIBRARY_PATH=${BUILD_DIR}/.heroku/vendor/lib:${LIBRARY_PATH}
    LD_LIBRARY_PATH=${BUILD_DIR}/.heroku/vendor/lib:${LD_LIBRARY_PATH}

EOF
